spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shopdb
    username: root
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
server:
  servlet:
    context-path: /api
app:
  storage:
    local-root: ./uploads
